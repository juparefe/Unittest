name: learn-github-actions
on: [push]
jobs:
  correr-unittest:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
      - run: unittest -v